import React from 'react';
import { Typography as MaterialTypography } from '@material-ui/core';

export default function Typography({ text }) {
    return <MaterialTypography>{text}</MaterialTypography>
}